# AGENTS.md

Front Next.js do EaiMesa. API: **eaimesa-backend**.

Não invente endpoint. Um único app: `/` landing, `/login` `/cadastro` `/painel/*`, `/{slug}` cardápio, `/admin` console.

`packages/shared` aqui é **cópia**. Fonte: `filiperamosds/eaimesa-backend/packages/shared`.

## Cursor Cloud

Sem teste de UI/desktop. `pnpm typecheck`. API em `:4000` no outro repo.
